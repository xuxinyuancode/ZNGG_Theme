module.exports = {
  purge: [],
  darkMode: 'class', // or 'media' or 'class'
  theme: {
    extend: {
      keyframes: {
      },
      animation: {
      }
    },
  },
  variants: {
    extend: {
      visibility: ['hover', 'focus']
    },
  },
  plugins: [],
}
